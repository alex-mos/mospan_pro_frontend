# mospan_pro_frontend

## Project setup

* rename **.env.example** to **.env** 

* install [docker](https://www.docker.com/)
 
* run:
```
docker network create --attachable mospan
docker-compose up
```
